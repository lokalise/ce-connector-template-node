const validate = async (key: string) => {
  // TODO: implementation
  return undefined
}

const refresh = async (refreshKey: string) => {
  // TODO: implementation
  return undefined
}

const authService = {
  validate,
  refresh,
}

export default authService
